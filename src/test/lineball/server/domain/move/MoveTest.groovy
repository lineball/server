package lineball.server.domain.move

import lineball.server.domain.dot.Dot
import spock.lang.Specification

class MoveTest extends Specification {
    def "IsAvailable"() {
        given:

        def dots = Arrays.asList( new Dot(0,0),
                                  new Dot(0,1
                                  ))
    }
}
